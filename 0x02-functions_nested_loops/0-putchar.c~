#include <unistd.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int _putchar(char c);
	char *str = "_putchar";

	for (int i = 0; str[i] != '\0'; i++)
{
	_putchar(str[i]);
}
	return (0)
}
int _putchar(char c)
{
	return write(1, &c, 1);
}
