#include "holberton.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int _putchar(char c);
	char *str = "_putchar\n";

	for (int i = 0; str[i] != '\0'; i++)
{
	_putchar(str[i]);
}
	return (0)
}
