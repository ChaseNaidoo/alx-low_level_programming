#include "main.h"

/**
 * _abs - Entry point
 *
 *@r: is the integer, multiply by negative 1 for positive/absolute value
 *
 * Return: (Absolute Value)
 */
int _abs(int r)
{
	if (r >= 0)
	{
	return (r);
	}
	else
	{
	return (r * -1);
	}
}
